Bootstrap: docker
From: ubuntu:20.04

%post
    apt-get update && 
    apt-get install -y g++ cmake libopenmpi-dev

%files
    ./ /project
    mkdir -p build
    cd build
    cmake ..
    cmake build .
    cp main ..
    cd ..

%runscript
    mpirun -np 2 /project/main
