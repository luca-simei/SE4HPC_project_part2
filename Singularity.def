Bootstrap: docker
From: ubuntu:20.04

%files
    ./ /project

%post
    export DEBIAN_FRONTED=noninteractive

    apt-get update && apt-get install -y \
        mpich \
        cmake \
        build-essentials \
        && apt-get clean

%runscript
    cd /project/build
    mpirun -np 2 ./main
