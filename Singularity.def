Bootstrap: docker
From: ubuntu:20.04

%post
    apt-get update && apt-get install -y mpich
    mkdir -p /app
    cp -r /project /app

%files
    ./ /project

%runscript
    cd /app/project/build
    mpirun -np 2 ./main
